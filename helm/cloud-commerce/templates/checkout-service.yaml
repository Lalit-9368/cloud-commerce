apiVersion: v1
kind: Service
metadata:
  name: checkout-service
  namespace: {{ .Values.global.namespace }}
spec:
  type: ClusterIP

  selector:
    app: checkout-service

  ports:
    - name: http
      port: 4003
      targetPort: 4003
      protocol: TCP