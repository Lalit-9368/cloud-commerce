apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: {{ .Values.global.namespace }}
spec:
  type: ClusterIP

  selector:
    app: catalog-service

  ports:
    - name: http
      port: 4002
      targetPort: 4002
      protocol: TCP